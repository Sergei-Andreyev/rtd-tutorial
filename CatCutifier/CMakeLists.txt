add_library (CatCutifier "CatCutifier.cpp" "CatCutifier.h" "about.cpp" "about.h")
target_include_directories(CatCutifier PUBLIC .)

# Should install library properly here, but I'm focusing on just the docs
